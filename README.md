# spa-overrides

A lightweight extension for overriding single-spa microfrontends.
Alternative to [single-spa Devtools Inspector](https://github.com/single-spa/single-spa-inspector).

## Building

Requires Deno 2.x.

- Just run `deno task build`.
    - To create a `.xpi` file for Firefox-based browsers, add `--target firefox`.
